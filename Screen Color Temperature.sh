# On Debian/Ubuntu distributions with the desktop environments other than Gnome or KDE Plasma install:
sudo apt install sct
# Set the temperature with:
sct [temperature]
# where temperature is a nmber between 1000 and 10000
# add sct [4000] to autostart
